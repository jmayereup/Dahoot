Use Tailwind CSS for styling. 
Never execute npm run deploy, ask the user to.
Use modals instead of alerts for a better mobile or limited browser experience.

For database schema changes, use `npm run db:setup` instead of PocketBase migrations.
